// // importing express package
// const express = require('express');

// // instantiating the app from express function
// const app = express();

/*
In the JavaScript file, write a program to perform a GET request on
the route https://coderbyte.com/api/challenges/json/rest-get-simple
and then print to the console the hobbies property in the following
format: ITEM1, ITEM2

*/

// async function printItems() {
//   const url = 'https://coderbyte.com/api/challenges/json/rest-get-simple';
//   const challengeCode = 'coderbyte';
//   const response = await fetch(url);
//   const data = await response.json();

//   const hobbies = data.hobbies;
//   const result = hobbies.map((hobby) => {
//     return `${hobby}:${challengeCode}`;
//   });
//   console.log(result.join(', '));
// }
// printItems();
